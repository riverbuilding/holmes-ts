# Phase 5 fixture evaluation — template

Generated report: `phase-5-fixture-evaluation-YYYY-MM-DD.json`

Record the command, Node version, package commit, model/base-URL identity (without credentials), prompt SHA-256, fixture SHA-256 values, and timezone. The generated JSON provides one safe summary per attempt; it deliberately contains no prompt, answer, or evidence body.

For each run, inspect the locally displayed answer and replace the reviewer placeholder with `pass`, `fail`, or `needs-review`. Include a short sanitized rationale and the material unsupported-claim count. A scenario passes only when all five attempts pass their automatic and human gates. Link any corrective change and replacement runs here.
